<?xml version="1.0" encoding="utf-8"?>
<!--
  Reworked to properly display SCORE rows from GameCenterOpenHelper:
  - username  (from USER table or fallback)
  - game      (_2048 / JUICE_DUNGEON_2)
  - score     (integer)
  - datetime  (string)
  - image     (per game)
-->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardUseCompatPadding="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Game image/banner -->
        <ImageView
            android:id="@+id/sportsImage"
            android:layout_width="0dp"
            android:layout_height="120dp"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:contentDescription="@string/app_name"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Dark gradient overlay to make text readable -->
        <View
            android:id="@+id/imageOverlay"
            android:layout_width="0dp"
            android:layout_height="120dp"
            android:background="#66000000"
            app:layout_constraintTop_toTopOf="@id/sportsImage"
            app:layout_constraintBottom_toBottomOf="@id/sportsImage"
            app:layout_constraintStart_toStartOf="@id/sportsImage"
            app:layout_constraintEnd_toEndOf="@id/sportsImage" />

        <!-- Username (top-left over image) -->
        <TextView
            android:id="@+id/username"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginTop="10dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="User"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="@id/sportsImage"
            app:layout_constraintStart_toStartOf="@id/sportsImage"
            app:layout_constraintEnd_toEndOf="@id/sportsImage" />

        <!-- Game name/tag (top-right over image) -->
        <TextView
            android:id="@+id/gameTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:layout_marginTop="10dp"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            android:text="_2048"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:background="#7A000000"
            app:layout_constraintTop_toTopOf="@id/sportsImage"
            app:layout_constraintEnd_toEndOf="@id/sportsImage" />

        <!-- Score big (bottom-left over image) -->
        <TextView
            android:id="@+id/score"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginBottom="10dp"
            android:text="0"
            android:textColor="@android:color/white"
            android:textSize="28sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/sportsImage"
            app:layout_constraintStart_toStartOf="@id/sportsImage" />

        <!-- Date/time (bottom-right over image) -->
        <TextView
            android:id="@+id/dateTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:layout_marginBottom="12dp"
            android:text="yyyy-MM-dd HH:mm:ss"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="@id/sportsImage"
            app:layout_constraintEnd_toEndOf="@id/sportsImage" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>